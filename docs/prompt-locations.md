# Prompt Locations

The AI Factory maintains one prompt file per AI tool per board column.
Each file is named after the tool and the normalized column name (e.g. `<tool>-<column>.md`).
A column with no matching prompt file is silently skipped by that tool.
