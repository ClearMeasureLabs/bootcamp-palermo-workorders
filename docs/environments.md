# Environments

- **TDD** — the continuous integration environment built on every commit to master.
- **UAT** — the pre-production environment used for validation before release.
- **Production** — the live environment serving real users.
