<a class="login-prompt-link"
   data-testid="@Elements.LoginLink"
   href="/login"
   aria-label="Sign in to the church portal — animated prompt when not signed in">Login</a>

@code
{
    public enum Elements
    {
        LoginLink
    }
}